{
  "name": "psd-explorer",
  "private": true,
  "version": "0.0.0",
  "author": "Leif Christian",
  "main": "dist/electron/main/main.js",
  "scripts": {
    "go": "npm run vite:build && tsc && electron .",
    "vite": "vite",
    "build": "bash devScript.sh",
    "ts": "tsc",
    "watch": "tsc -w",
    "buildtheapp": "npm run vite:build && tsc && electron-builder"
  },
  "debug": {
    "env": {
      "VITE_DEV_SERVER_URL": ""
    }
  },
  "build": {
    "appId": "none",
    "asar": true,
    "directories": {
      "buildResources": "assets",
      "output": "release/${version}"
    },
    "files": [
      "dist"
    ],
    "mac": {
      "artifactName": "${productName}_${version}.${ext}",
      "target": [
        "dmg"
      ]
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "${productName}_${version}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "deleteAppDataOnUninstall": false
    }
  },
  "dependencies": {
    "@popperjs/core": "^2.11.6",
    "bash": "^0.0.1",
    "bootstrap": "^5.2.3",
    "dotenv": "^16.0.3",
    "esbuild": "^0.16.8",
    "mime-types": "^2.1.35",
    "os": "^0.1.2",
    "vite-plugin-electron": "^0.10.4",
    "vite-plugin-electron-renderer": "^0.11.3",
    "vue": "^3.2.45"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.3.4",
    "concurrently": "^7.6.0",
    "electron": "^20.3.8",
    "electron-builder": "^23.6.0",
    "typescript": "^4.9.4",
    "vite": "^2.9.15",
    "vue-router": "^4.1.6",
    "vue-tsc": "^0.34.17"
  }
}
